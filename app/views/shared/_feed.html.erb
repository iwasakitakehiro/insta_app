<% if @feed_items.any? %>
<h3>みんなの投稿</h3>
  <ol class="microposts">
    <%= render @feed_items %>
  </ol>
  <%= will_paginate @feed_items,
                    params: { controller: :sessions, action: :new } %>
<% end %>