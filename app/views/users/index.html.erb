<% provide(:title, 'All users') %>
<h1>All users</h1>

<ul class="users">
  <% @users.each do |user| %>
  <%= render @users %>
  <div id="like-btn-<%= micropost.id %>">
   <%= render 'likes/like', micropost: micropost %>
  </div>
  <% end %>
</ul>
